flask>=3.0.0
flask-talisman>=1.1.0
flask-cors>=4.0.0
faster-whisper>=1.0.0
transformers>=4.40.0
torch>=2.0.0
sentencepiece>=0.2.0
sacremoses>=0.1.0
nltk>=3.8.0
pandas>=2.0.0
requests>=2.31.0
python-dotenv>=1.0.0
gunicorn>=21.0.0
# Pronunciation layer (Feature 1/2). Optional at runtime — g2p_medical
# falls back to a heuristic if g2p-en or its NLTK data are unavailable.
g2p-en>=2.1.0
eng-to-ipa>=0.0.2
