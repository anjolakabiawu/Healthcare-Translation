version: '3.8'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - model_cache:/root/.cache/huggingface
    environment:
      - FLASK_ENV=production
      - SECRET_KEY=${SECRET_KEY}
      - WHISPER_MODEL_SIZE=base
volumes:
  model_cache:
